# 1주차 기능정리

## 1. 주차 목표 및 결과

1주차 목표는 **플레이어 기본 조작과 기본 전투 뼈대 구현**이다.

현재 플레이어의 입력, TPS 카메라, 이동, 기본 공격, 회피, 피해, 사망까지 하나의 흐름으로 연결했으며 전체 통합 테스트를 완료했다.

```text
1주차 진행도: 100%
```

---

## 2. 입력 시스템

Unity Input System을 기반으로 입력을 분리했다.

현재 입력 구성:

```text
Move      - WASD
Look      - Mouse Delta
Attack    - 마우스 왼쪽 버튼
Dodge     - Left Shift
Skill1    - 숫자 1
Skill2    - 숫자 2
Skill3    - 숫자 3
Interact  - E
```

`PlayerInputReader`가 입력을 받아 다른 플레이어 컴포넌트에 전달한다.

- 이동과 시점 입력은 값으로 저장한다.
- 공격, 회피, 스킬, 상호작용 입력은 이벤트로 전달한다.
- 각 플레이어 기능이 Input System을 직접 처리하지 않도록 분리했다.

---

## 3. TPS 카메라와 캐릭터 회전

`PlayerCameraController`에서 마우스 시점 입력과 캐릭터 회전을 처리한다.

구현된 기능:

- Cinemachine 기반 3인칭 백뷰 카메라
- 마우스 좌우 입력에 따른 카메라 및 캐릭터 몸 회전
- 마우스 상하 입력에 따른 카메라 Pitch 조절
- 카메라 상하 각도 제한
- 게임 시작 시 마우스 커서 고정
- 사망 후 카메라와 캐릭터 회전 정지

현재 캐릭터는 카메라가 바라보는 방향을 몸의 정면으로 사용한다.

---

## 4. 플레이어 이동

`PlayerMoveController`와 `CharacterController`를 사용해 플레이어 이동을 구현했다.

이동 방식:

```text
W - 카메라 기준 전진
S - 몸을 돌리지 않고 후진
A - 몸 방향을 유지하고 왼쪽 이동
D - 몸 방향을 유지하고 오른쪽 이동
```

구현된 기능:

- 카메라 기준 이동 방향 계산
- 대각선 이동 시 속도 증가 방지
- 중력 처리
- 회피 중 일반 이동 차단
- 사망 중 이동 차단
- 공격 중 이동 허용

현재 이동 규칙:

```text
일반 상태 - 이동 가능
공격 중   - 이동 가능
회피 중   - 이동 불가
사망 중   - 이동 불가
```

---

## 5. 회피 시스템

`PlayerDodgeController`에서 회피 입력, 방향, 지속시간과 쿨타임을 관리한다.

구현된 기능:

- 이동 입력 방향으로 회피
- 방향 입력이 없으면 캐릭터 정면으로 회피
- 카메라 기준 회피 방향 계산
- `CharacterController`를 통한 회피 이동
- 회피 지속시간과 쿨타임
- 회피 중 재회피 방지
- 회피 중 일반 이동 차단
- 회피 중 공격 차단
- 공격 중 회피 허용
- 공격 중 회피하면 진행 중인 공격 취소
- 사망 중 회피 차단
- 사망하면 진행 중인 회피 종료

현재 설정값:

```text
dodgeSpeed: 15
dodgeDur: 0.4
dodgeCoolTime: 0.8
```

---

## 6. 회피 무적

`Health`가 `PlayerDodgeController.IsDodging`을 확인해 회피 중 들어오는 피해를 무시한다.

```text
회피 중 피해 요청
-> Health가 회피 상태 확인
-> 피해 무시
```

회피가 끝나면 다시 정상적으로 피해를 받는다.

현재는 플레이어에게 적합한 간단한 구조다. 적, 버프, 피격 후 무적 등이 추가되면 별도의 무적 상태 시스템으로 확장할 수 있다.

---

## 7. 체력과 피해 시스템

`IDamageable`과 `Health`를 통해 공통 피해 처리 구조를 만들었다.

### IDamageable

- 피해를 받을 수 있는 대상의 공통 규칙
- `TakeDamage(float damage)` 정의

### Health

- 최대 체력과 현재 체력 관리
- 전달받은 피해량만큼 현재 체력 감소
- 현재 체력이 0 미만으로 내려가지 않도록 제한
- 회피 중 피해 무시
- 중복 사망 처리 방지
- 현재 체력이 0이 되면 사망 상태로 전환
- 외부에서 현재 체력과 사망 상태를 읽을 수 있는 프로퍼티 제공

주요 상태:

```text
maxHealth
currentHealth
CurrentHealth
isDead
IsDead
```

테스트용 Context Menu를 이용해 피해와 사망 흐름을 확인했다.

---

## 8. 기본 공격

`PlayerCombat`에서 기본 공격 입력과 공격 상태를 관리한다.

구현된 기능:

- 마우스 왼쪽 버튼 기본 공격
- 공격 중 중복 공격 입력 방지
- 공격 쿨타임
- 회피 중 공격 차단
- 사망 중 공격 차단
- 공격 중 이동 허용
- 공격 중 회피 허용
- 회피 시작 시 진행 중인 공격 취소
- 공격 취소 시 공격 Collider 즉시 비활성화

현재 설정값:

```text
attackDamage: 20
attackCoolTime: 0.5
```

현재는 단타 기본 공격 구조다.

---

## 9. 공격 판정

플레이어 자식 오브젝트인 `AttackArea`가 실제 공격 범위를 담당한다.

Hierarchy 구조:

```text
Player
└─ AttackArea
   ├─ BoxCollider
   ├─ Rigidbody
   └─ AttackArea 컴포넌트
```

설정:

```text
BoxCollider
-> Is Trigger: On

Rigidbody
-> Use Gravity: Off
-> Is Kinematic: On

Target Layer
-> Enemy
```

구현된 기능:

- 공격 중에만 Collider 활성화
- 공격 종료 시 Collider 비활성화
- LayerMask로 공격 대상 판별
- `IDamageable`을 구현한 대상에게 피해 전달
- 한 번의 공격으로 같은 대상에게 중복 피해 방지
- 공격 취소 또는 컴포넌트 비활성화 시 판정 초기화

---

## 10. Animation Event 기반 공격 타이밍

공격 판정은 코루틴이나 고정 시간이 아니라 Animation Event와 연결했다.

```text
검을 휘두르기 시작
-> EnableAttackArea()

검을 휘두르는 구간 종료
-> DisableAttackArea()

공격 모션 종료
-> EndAttack()
```

따라서 실제 검을 휘두르는 프레임과 공격 판정이 맞춰진다.

공격 중 회피 흐름:

```text
회피 입력
-> CancelAttack()
-> isAttacking = false
-> AttackArea 비활성화
-> 회피 시작
```

---

## 11. 플레이어 애니메이션

`PlayerAnimationController`가 다른 플레이어 컴포넌트의 상태를 읽고 Animator에 전달한다.

현재 연결된 애니메이션:

```text
Idle
Running
Attack
Dodge
Death
```

Animator 파라미터:

```text
Float   MoveX
Float   MoveY
Float   MoveAmount
Bool    IsMoving
Bool    IsDodging
Bool    IsAttacking
Bool    IsDead
Trigger Attack
```

역할 분리:

```text
PlayerMoveController
-> 실제 이동

PlayerDodgeController
-> 실제 회피

PlayerCombat
-> 실제 공격 상태와 판정

PlayerAnimationController
-> 위 상태를 읽고 애니메이션 제어
```

Animator의 `Apply Root Motion`은 꺼져 있으며 실제 이동은 `CharacterController`가 담당한다.

---

## 12. 사망 처리

현재 체력이 0이 되면 모든 플레이어 행동이 정지한다.

사망 흐름:

```text
Health.TakeDamage()
-> currentHealth가 0
-> Die()
-> IsDead가 true
-> 이동 차단
-> 카메라 회전 차단
-> 공격 취소 및 차단
-> 회피 종료 및 차단
-> Animator IsDead 활성화
-> Death_B 재생
-> 마지막 프레임 유지
```

현재 사망 애니메이션:

```text
Death_B
Loop Time: Off
```

`DeathPose`를 별도로 연결했을 때 `Any State -> Death`가 다시 실행되며 사망 동작이 반복되는 문제가 있었다.

현재 해결 방식:

```text
Death -> DeathPose 전환 제거
Death_B 재생 완료
-> Death 상태에 계속 머무름
-> Death_B 마지막 프레임 유지
```

테스트 완료 상황:

- Idle 중 사망
- 이동 중 사망
- 공격 중 사망
- 회피 중 사망
- 사망 후 이동 입력
- 사망 후 카메라 입력
- 사망 후 공격 입력
- 사망 후 회피 입력

---

## 13. 플레이어 행동 규칙

| 상태 | 이동 | 공격 | 회피 | 피해 |
|---|---:|---:|---:|---:|
| 일반 | 가능 | 가능 | 가능 | 받음 |
| 공격 중 | 가능 | 중복 불가 | 가능 | 받음 |
| 회피 중 | 불가 | 불가 | 재회피 불가 | 무시 |
| 사망 | 불가 | 불가 | 불가 | 추가 처리 없음 |

상태 간 상호작용:

```text
공격 중 회피
-> 공격 취소 후 회피

회피 중 공격
-> 공격 입력 무시

사망 중 공격 또는 회피
-> 입력 무시

공격 또는 회피 중 사망
-> 해당 행동 즉시 종료
```

---

## 14. 보류한 기능

### 기본 공격 콤보

예정 후보:

```text
1타: Melee_1H_Attack_Slice_Horizontal
2타: Melee_1H_Attack_Slice_Diagonal
3타: Melee_1H_Attack_Chop
```

적 피격과 전투 흐름을 먼저 완성한 뒤 다음 요소와 함께 구현할 예정이다.

- 콤보 단계
- 입력 버퍼
- 콤보 입력 가능 구간
- 타격별 Animation Event
- 회피 및 사망 시 콤보 초기화

### 스킬 시스템

입력 이벤트는 준비되어 있지만 실제 스킬은 아직 구현하지 않았다.

```text
Skill1
Skill2
Skill3
```

플레이어 기본 전투와 적 전투가 안정된 뒤 구현할 예정이다.

### 방패

현재 전사 모델에 포함된 방패는 기능성 장비가 아니며 스크립트 참조도 없다. 방어 시스템을 만들 계획이 없으므로 방패 모델을 비활성화하는 방향으로 결정했다.

---

## 15. 1주차 완료 항목

```text
[x] Input System
[x] TPS 카메라
[x] CharacterController 이동
[x] 카메라 기준 이동 방향
[x] 회피 이동
[x] 회피 쿨타임
[x] 회피 무적
[x] Health / IDamageable
[x] 기본 공격
[x] AttackArea 공격 판정
[x] 중복 피해 방지
[x] Animation Event 타격 타이밍
[x] 공격 중 회피 취소
[x] 상태별 행동 제한
[x] Idle / Running / Attack / Dodge 애니메이션
[x] 사망 상태 및 Death_B 애니메이션
[x] 전체 통합 테스트
```

---

## 16. 다음 단계

2주차에는 적 캐릭터의 기본 전투 구조를 구현한다.

권장 순서:

```text
Enemy Health
-> 플레이어 감지
-> 플레이어 추적
-> 공격 범위 진입
-> 적 공격
-> 플레이어 피격
-> 적 피격 및 사망
-> 기본 전투 한 사이클 테스트
```
