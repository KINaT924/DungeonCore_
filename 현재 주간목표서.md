# 현재 주간목표서

## 현재 개발 단계

```text
전체 일정: 8주
완료 주차: 1주차
현재 주차: 2주차
현재 목표: 전사 전투 시스템 완성
```

이 문서는 현재 주차의 작업만 자세히 관리한다. 주차가 끝나면 완료 내용을 `8주간의 계획서.md`에 반영하고 다음 주차 목표로 내용을 교체한다.

체크 표시 기준:

```text
[x] 구현과 기본 테스트 완료
[ ] 미구현 또는 테스트 미완료
[~] 일부 구현됐지만 추가 작업 필요
```

---

# 2주차 목표

전사가 AI 없는 몬스터 더미를 기본 공격과 스킬로 공격하고 처치할 수 있으며, 플레이어의 체력 변화를 UI로 확인할 수 있어야 한다.

최종 흐름:

```text
전사 기본 공격 또는 스킬
-> MonsterDummy 피격
-> MonsterDummy 체력 감소
-> 체력 0에서 사망

플레이어 피해
-> 플레이어 체력 감소
-> 체력 UI 갱신
-> 체력 0에서 사망
```

---

# 현재까지 완료된 기반 기능

다음 항목은 1주차에 완료했으며 2주차 전투 개발에서 재사용한다.

- [x] PlayerInputReader
- [x] TPS 카메라
- [x] CharacterController 이동
- [x] 전사 기본 공격 입력
- [x] 기본 공격 쿨타임
- [x] AttackArea 공격 판정
- [x] Animation Event 기반 공격 판정
- [x] 한 번의 공격에서 중복 피해 방지
- [x] IDamageable
- [x] Health
- [x] 회피
- [x] 회피 중 무적
- [x] 공격 중 회피 취소
- [x] 사망 상태
- [x] 사망 후 행동 차단
- [x] Idle, Running, Attack, Dodge, Death 애니메이션
- [x] 전체 플레이어 상태 통합 테스트

---

# 작업 1: MonsterDummy 구성

목적: AI 없이 공격, 피해, 사망을 반복 테스트할 수 있는 고정 대상을 만든다.

권장 Hierarchy:

```text
MonsterDummy
├─ Model
├─ Collider
└─ Health
```

작업 항목:

- [x] `SampleScene`에 `Enemy_Dummy` 구성
- [x] 테스트용 모델 연결
- [x] Enemy Layer 지정
- [x] Collider 설정
- [x] Health 컴포넌트 연결
- [x] 최대 체력 100 설정
- [x] 플레이어 AttackArea의 Target Layer 확인
- [x] 플레이어 기본 공격으로 피해 적용 확인
- [x] 공격 한 번에 피해가 한 번만 적용되는지 확인
- [x] 체력 0에서 사망 상태 확인
- [x] 사망 후 추가 피해가 적용되지 않는지 확인
- [ ] 필요하면 간단한 사망 표시 적용 — 선택 사항, 실제 몬스터 사망 연출은 3주차

완료 기준:

```text
전사가 MonsterDummy를 공격
-> 공격 1회당 피해 1회
-> 체력 감소
-> 체력 0에서 사망
```

---

# 작업 2: Health 구조 보완

목적: UI와 다른 시스템이 Health 내부 값을 안전하게 확인하고 체력 변화에 반응할 수 있게 한다.

현재 존재하는 항목:

```text
maxHealth
currentHealth
CurrentHealth
IsDead
TakeDamage()
```

추가 검토 항목:

- [x] `MaxHealth` 외부 읽기 프로퍼티
- [x] 체력 변경 이벤트 `OnHealthChanged`
- [x] 사망 이벤트 `OnDied`
- [x] UI가 활성화될 때 현재 체력을 직접 읽는 초기화 방식 적용
- [x] 플레이어와 Enemy_Dummy가 같은 Health를 사용할 수 있는지 확인
- [x] Enemy_Dummy에 `PlayerDodgeController`가 없어도 정상 작동하는지 확인
- [x] 테스트용 `Take Test Damege`는 UI 테스트가 끝날 때까지 유지

완료 기준:

```text
Health의 체력이 변경될 때
-> UI 또는 외부 시스템이 변경 사실을 전달받음
-> Health 내부 값은 외부에서 직접 변경할 수 없음
```

---

# 작업 3: 플레이어 체력 UI

목적: 플레이어의 현재 체력을 화면에서 확인할 수 있게 한다.

권장 구조:

```text
Canvas
└─ PlayerHUD
   └─ HealthBar
      ├─ Background
      └─ Fill
```

작업 항목:

- [x] `GameCanvas`, `GameHUD`, `PlayerStatus` 구성
- [x] 0~1 비율 방식 Slider 적용
- [x] HealthBar와 TMP 체력 숫자 UI 제작
- [~] Health 참조 연결 — 현재 씬에서는 `Enemy_Dummy Health`가 연결되어 있어 `Player Health`로 교체 필요
- [x] 게임 시작 시 현재 체력과 최대 체력으로 초기화하는 코드 적용
- [ ] 플레이어 피해를 받으면 체력바가 감소하는지 재검증
- [ ] 플레이어 체력이 0이면 UI도 0을 유지하는지 재검증
- [x] Canvas Scaler 1920×1080, Match 0.5 적용
- [ ] 플레이어 사망 흐름과 UI 통합 테스트
- [x] 표시 전용 Image 및 TMP Text의 Raycast Target 비활성화
- [x] 새 Input System용 `InputSystemUIInputModule` 확인
- [x] `GameHUD.prefab` 생성

완료 기준:

```text
플레이어 피해
-> Health 감소
-> 같은 시점에 체력 UI 감소
-> 체력 0에서 사망 및 UI 0 표시
```

---

# 작업 4: 전사 스킬 1개

목적: 기본 공격과 구분되는 전사 전용 스킬 하나를 완성한다.

우선 후보:

```text
전방 강공격
넓은 범위 내려찍기
회전 베기
짧은 돌진 공격
```

스킬은 구현 전에 다음을 확정한다.

- [ ] 스킬 종류
- [ ] 사용할 입력: Skill1
- [ ] 사용할 애니메이션
- [ ] 피해량
- [ ] 공격 범위
- [ ] 쿨타임
- [ ] 스킬 중 이동 가능 여부
- [ ] 스킬 중 회피 가능 여부
- [ ] 회피 시 스킬 취소 여부
- [ ] 피격 시 스킬 취소 여부

구현 항목:

- [ ] PlayerCombat과 스킬 책임 분리 방식 결정
- [ ] Skill1 입력 이벤트 연결
- [ ] 스킬 사용 가능 조건
- [ ] 스킬 쿨타임
- [ ] 스킬 상태 외부 읽기 프로퍼티
- [ ] 스킬 애니메이션 파라미터
- [ ] 스킬 Animator 상태 및 Transition
- [ ] Animation Event로 공격 범위 활성화
- [ ] Animation Event로 공격 범위 비활성화
- [ ] Animation Event로 스킬 종료
- [ ] MonsterDummy 피해 적용
- [ ] 회피 시 취소 처리
- [ ] 사망 시 취소 처리
- [ ] 스킬 도중 기본 공격 입력 규칙 확인

완료 기준:

```text
Skill1 입력
-> 스킬 애니메이션
-> 지정된 프레임에 공격 판정
-> MonsterDummy 피해
-> 스킬 종료
-> 쿨타임 후 재사용
```

---

# 작업 5: 2주차 통합 테스트

- [ ] 기본 공격으로 MonsterDummy 피해
- [ ] 기본 공격 한 번에 중복 피해 없음
- [ ] 기본 공격으로 MonsterDummy 처치
- [ ] 전사 스킬로 MonsterDummy 피해
- [ ] 전사 스킬 쿨타임 정상 작동
- [ ] 스킬 중 상태 충돌 없음
- [ ] 회피 시 공격 또는 스킬 취소 규칙 정상 작동
- [ ] 플레이어 피해 시 체력 UI 감소
- [ ] 플레이어 체력 0에서 사망
- [ ] 플레이어 사망 후 모든 행동 차단
- [ ] MonsterDummy 사망 후 추가 피해 방지
- [ ] Console 오류 없음

---

# 이번 주에 구현하지 않는 기능

다음 기능은 계획에서 삭제된 것이 아니라 이후 주차로 미룬 기능이다.

```text
거너 플레이어
-> 6주차

전사 3연타 기본 공격
-> 6주차 거너 제작 시기와 함께 구현

로비 캐릭터 선택
-> 6주차

몬스터 추적 AI와 리젠
-> 3주차

코어 시스템
-> 4주차

성장 카드
-> 5주차

2인 멀티플레이
-> 싱글 MVP 완성 후 시간이 남으면 구현
```

---

# 현재 진행 현황

| 구분 | 상태 |
|---|---|
| 1주차 전사 기반 | 완료 |
| MonsterDummy | 완료 |
| Health 이벤트 구조 | 완료 |
| 플레이어 체력 UI | 부분 완료 — Health 참조 수정 및 재테스트 필요 |
| 전사 스킬 1개 | 미착수 |
| 2주차 통합 테스트 | 미착수 |

현재 즉시 수정할 작업:

```text
SampleScene의 GameHUD/PlayerStatus/HealthBar 선택
-> PlayerHealthUI.health에 연결된 Enemy_Dummy Health 제거
-> Player 오브젝트의 Health 연결
-> Player 피해 시 체력 UI 감소 및 사망 흐름 재테스트
```

위 참조 수정과 테스트가 완료되면 다음 본 작업은 다음과 같다.

```text
전사 스킬 1개 기획 확정
-> 스킬 상태 규칙 결정
-> 스킬 입력, 쿨타임, 애니메이션, 판정 구현
```

---

# 문서 갱신 규칙

1. 기능을 실제로 구현하고 Unity Play Mode에서 테스트한다.
2. 정상 작동이 확인된 항목만 `[x]`로 변경한다.
3. 일부 구현 상태는 `[~]`와 함께 남은 내용을 적는다.
4. 새로운 기능을 시작하기 전에 현재 작업의 완료 기준을 먼저 확인한다.
5. 현재 주차가 끝나면 완료 내용을 `8주간의 계획서.md`에 반영한다.
6. 다음 주차 시작 시 이 문서를 해당 주차 목표로 교체한다.
7. 일정 변경 시 기능을 삭제하지 말고 이동한 주차와 이유를 기록한다.
