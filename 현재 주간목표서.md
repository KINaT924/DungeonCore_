# 현재 주간목표서

## 현재 개발 단계

```text
전체 일정: 8주
완료 주차: 1주차
현재 주차: 2주차
현재 목표: 전사 전투 시스템 완성
```

이 문서는 현재 주차의 상세 목표·진행 현황과 다음 작업 인수인계를 함께 관리하는 기준 문서다. 주차 중에도 범위가 바뀌면 `8주간의 계획서.md`를 함께 갱신한다. 주차가 끝나면 완료 내용을 해당 계획서에 반영하고 다음 주차 목표로 내용을 교체한다. 다음 대화는 별도 인수인계 파일 대신 이 문서의 최신 인수인계와 미적용 코드부터 확인한다.

체크 표시 기준:

```text
[x] 구현과 기본 테스트 완료
[ ] 미구현 또는 테스트 미완료
[~] 일부 구현됐지만 추가 작업 필요
```

---

# 2주차 목표

기존 기본 공격을 유지하면서 Skill1 Slash와 Skill2 Spin을 완성해 더미에 피해를 주고 처치한다. 체력 UI는 완료됐으며 스킬과의 통합을 확인한다. 기본 공격의 새 모션·범위 작업은 보류한다.

최종 흐름:

```text
전사 기본 공격 또는 스킬
-> MonsterDummy 피격
-> MonsterDummy 체력 감소
-> 체력 0에서 사망

플레이어 피해
-> 플레이어 체력 감소
-> 체력 UI 갱신
-> 체력 0에서 사망
```

---

# 현재까지 완료된 기반 기능

다음 항목은 1주차에 완료했으며 2주차 전투 개발에서 재사용한다.

- [x] PlayerInputReader
- [x] TPS 카메라
- [x] CharacterController 이동
- [x] 전사 기본 공격 입력
- [x] 기본 공격 쿨타임
- [x] AttackArea 공격 판정
- [x] Animation Event 기반 공격 판정
- [x] 한 번의 공격에서 중복 피해 방지
- [x] IDamageable
- [x] Health
- [x] 회피
- [x] 회피 중 무적
- [x] 공격 중 회피 취소
- [x] 사망 상태
- [x] 사망 후 행동 차단
- [x] Idle, Running, Attack, Dodge, Death 애니메이션
- [x] 전체 플레이어 상태 통합 테스트

---

# 작업 1: MonsterDummy 구성

목적: AI 없이 공격, 피해, 사망을 반복 테스트할 수 있는 고정 대상을 만든다.

권장 Hierarchy:

```text
MonsterDummy
├─ Model
├─ Collider
└─ Health
```

작업 항목:

- [x] `SampleScene`에 `Enemy_Dummy` 구성
- [x] 테스트용 모델 연결
- [x] Enemy Layer 지정
- [x] Collider 설정
- [x] Health 컴포넌트 연결
- [x] 최대 체력 100 설정
- [x] 플레이어 AttackArea의 Target Layer 확인
- [x] 플레이어 기본 공격으로 피해 적용 확인
- [x] 공격 한 번에 피해가 한 번만 적용되는지 확인
- [x] 체력 0에서 사망 상태 확인
- [x] 사망 후 추가 피해가 적용되지 않는지 확인
- [ ] 필요하면 간단한 사망 표시 적용 — 선택 사항, 실제 몬스터 사망 연출은 3주차

완료 기준:

```text
전사가 MonsterDummy를 공격
-> 공격 1회당 피해 1회
-> 체력 감소
-> 체력 0에서 사망
```

---

# 작업 2: Health 구조 보완

목적: UI와 다른 시스템이 Health 내부 값을 안전하게 확인하고 체력 변화에 반응할 수 있게 한다.

현재 존재하는 항목:

```text
maxHealth
currentHealth
CurrentHealth
IsDead
TakeDamage()
```

추가 검토 항목:

- [x] `MaxHealth` 외부 읽기 프로퍼티
- [x] 체력 변경 이벤트 `OnHealthChanged`
- [x] 사망 이벤트 `OnDied`
- [x] UI가 활성화될 때 현재 체력을 직접 읽는 초기화 방식 적용
- [x] 플레이어와 Enemy_Dummy가 같은 Health를 사용할 수 있는지 확인
- [x] Enemy_Dummy에 `PlayerDodgeController`가 없어도 정상 작동하는지 확인
- [x] 테스트용 `Take Test Damege`는 UI 테스트가 끝날 때까지 유지

완료 기준:

```text
Health의 체력이 변경될 때
-> UI 또는 외부 시스템이 변경 사실을 전달받음
-> Health 내부 값은 외부에서 직접 변경할 수 없음
```

---

# 작업 3: 플레이어 체력 UI

목적: 플레이어의 현재 체력을 화면에서 확인할 수 있게 한다.

권장 구조:

```text
Canvas
└─ PlayerHUD
   └─ HealthBar
      ├─ Background
      └─ Fill
```

작업 항목:

- [x] `GameCanvas`, `GameHUD`, `PlayerStatus` 구성
- [x] 0~1 비율 방식 Slider 적용
- [x] HealthBar와 TMP 체력 숫자 UI 제작
- [x] Player Health 참조 연결 확인
- [x] 게임 시작 시 현재 체력과 최대 체력으로 초기화하는 코드 적용
- [x] 플레이어 피해 시 체력바·숫자 감소 — 사용자 정상 동작 확인
- [x] 플레이어 체력 0에서 UI도 0 유지 — 사용자 확인
- [x] Canvas Scaler 1920×1080, Match 0.5 적용
- [x] 플레이어 사망 흐름과 UI 통합 테스트 — 사용자 확인
- [x] 표시 전용 Image 및 TMP Text의 Raycast Target 비활성화
- [x] 새 Input System용 `InputSystemUIInputModule` 확인
- [x] `GameHUD.prefab` 생성
- [x] 시작 시 0/100 표시 문제 보정 — OnEnable 초기화 유지, Start에서 InitializeHealthUI 재호출
- 사용자가 체력 UI 크기·위치를 조정했다. 현재 배치 유지.

완료 기준:

```text
플레이어 피해
-> Health 감소
-> 같은 시점에 체력 UI 감소
-> 체력 0에서 사망 및 UI 0 표시
```

---

# 작업 4: 전사 스킬 2개

2026-09-05 사용자 결정으로 스킬 1개에서 2개로 확대했다. 적합한 Spin 모션을 확보한 것이 변경 이유다.

| 구분 | 의도한 Animator 상태 | 클립 | 입력 |
|---|---|---|---|
| Skill1 | Skill1_SlashAttack | Attack_Slash | 1 / Skill1 |
| Skill2 | Skill2_SpinAttack | Attack_Spin | 2 / Skill2 |

- 모션 파일 확인: Slash 약 0.97초, Spin 1.2초, 모두 30 FPS 및 반복 꺼짐.
- 갱신 시 저장된 Animator에서 Slash 상태 이름은 Skill2_SlashAttack으로 확인됐다. 사용자 의도 Skill1_SlashAttack과 다르므로 연결 단계에서 확인한다. 자동 변경하지 않았다.
- Skill1/Skill2 파라미터와 Spin 상태는 존재하지만 실제 입력 시전 완료를 뜻하지 않는다.

작업 항목:

- [~] 두 스킬 모션 및 Animator 준비 — 파일·상태 존재, 이름·전환·실제 재생 검증 필요
- [ ] PlayerSkillController 작성 — 전체 코드 안내만 완료, 실제 파일 없음
- [ ] Skill1/Skill2 입력 이벤트 구독 및 Inspector 연결
- [ ] 각 스킬 개별 쿨타임 구현·테스트 (초기 제안 5초 / 8초, 확정 밸런스 아님)
- [ ] 회피·사망 중 입력 차단 확인
- [ ] 시전 상태 및 스킬 간 동시 사용 차단
- [ ] 스킬 중 기본 공격 차단과 이동 규칙 연결
- [ ] PlayerAnimationController에서 각 스킬 Trigger 연동
- [ ] 스킬별 공격 범위 구성·피해량 확정
- [ ] Animation Event로 판정 활성·비활성 및 스킬 종료
- [ ] 회피·사망 시 스킬 취소와 판정 종료
- [ ] 더미 피해·처치 및 스킬당 적마다 중복 피해 방지

설계안과 미확정 사항:

- Skill1 전방, Skill2 주변 범위. 검 모델 길이에 제한하지 않는다.
- 이펙트는 추후 추가한다. 현재 입력·쿨타임 작성의 선행 조건이 아니다.
- Skill1Area(Box), Skill2Area(Sphere) 구성법은 안내했지만 사용자의 완료 확인은 없다.
- 스킬 중 이동 불가, 회피로 취소, 일반 피격 시 유지, 사망 시 취소, 취소 후 쿨타임 유지 방향을 제안했다. 실제 연결·검증 전이다.
- Spin은 우선 사용 1회당 적마다 피해 1회인 설계안. 피해량·최종 범위는 테스트로 조정한다.

완료 기준: 각 입력 → 사용 조건 검사 → 애니메이션 → 지정 타격 구간 피해 → 종료/취소 → 개별 쿨타임 후 재사용.

---
# 작업 5: 2주차 통합 테스트

- [ ] 기본 공격으로 MonsterDummy 피해
- [ ] 기본 공격 한 번에 중복 피해 없음
- [ ] 기본 공격으로 MonsterDummy 처치
- [ ] Skill1·Skill2 각각 MonsterDummy 피해·처치
- [ ] 두 스킬 개별 쿨타임 및 동시 시전 차단 정상 작동
- [ ] 스킬 중 상태 충돌 없음
- [ ] 회피 시 공격 또는 스킬 취소 규칙 정상 작동
- [ ] 플레이어 피해 시 체력 UI 감소
- [ ] 플레이어 체력 0에서 사망
- [ ] 플레이어 사망 후 모든 행동 차단
- [ ] MonsterDummy 사망 후 추가 피해 방지
- [ ] Console 오류 없음

---

# 이번 주에 구현하지 않는 기능

다음 기능은 계획에서 삭제된 것이 아니라 이후 주차로 미룬 기능이다.

```text
거너 플레이어
-> 6주차

전사 3연타 기본 공격
-> 6주차 거너 제작 시기와 함께 구현

로비 캐릭터 선택
-> 6주차

몬스터 추적 AI와 리젠
-> 3주차

코어 시스템
-> 4주차

성장 카드
-> 5주차

2인 멀티플레이
-> 싱글 MVP 완성 후 시간이 남으면 구현
```

---

# 현재 진행 현황

최종 갱신: 2026-09-05. `[x]`는 구현과 기본 테스트까지 확인된 경우에만 기록한다. 파일 존재나 코드 안내만으로 완료 처리하지 않는다. 체력 UI 완료는 사용자의 정상 동작 보고를 근거로 하며, 이번 문서 갱신에서 Unity 실행 테스트를 대신 수행한 것은 아니다.

| 구분 | 상태 |
|---|---|
| 1주차 전사 기반 | 완료 |
| MonsterDummy | 완료 |
| Health 이벤트 구조 | 완료 |
| 플레이어 체력 UI | 완료 — 사용자 시작·피해·사망 정상 확인, 사용자 배치 유지 |
| 스킬 2개 모션/Animator | 부분 준비 — 명칭 확인·연동·검증 남음 |
| 입력·개별 쿨타임 | 미적용 — 스크립트 작성 단계에서 중단 |
| 시전 상태·판정·취소 | 미완료 |
| 2주차 전체 통합 테스트 | 미완료 — 개별 완료 기능도 스킬 통합 후 재검증 |

## 최신 인수인계 — 여기서 다음 작업 재개

**PlayerSkillController 전체 코드는 안내했지만 사용자가 아직 작성·적용하지 않았다. 다음 대화는 스크립트 작성부터 시작한다.**

- 기본 공격의 모션·범위 변경은 보류하고 두 스킬부터 구현한다. 기존 기본 공격 삭제·비활성화 요청은 아니다.
- 현재 KayKit SD 전사·거너를 유지한다. 모델 교체를 재추진하지 않는다.
- Map Group은 도시 맵으로 사용자가 제작했다. 조명을 어둡게 구성하고 몬스터도 과도하게 크게 만들지 않을 계획이다.
- Mixamo는 X Bot 정상/전사 뒤틀림을 확인했으나 Chest 보완으로 개선되지 않았다. 원인이 확정되거나 해결됐다고 하지 않는다. Inspector 오류는 사용자 보고로 사라졌다. 현재 모션을 기준으로 진행한다.
- 사용자 학습 방식: 전체 코드와 역할·이유 주석 → 정확한 Inspector 연결 → 테스트 → 사용자 결과 확인 → 다음 단계.
- 기능별 학습 효과와 포트폴리오 장점·한계도 설명한다. 사용자 네이밍과 체력 UI 배치를 유지한다.
- 명시적 적용 요청이 없으면 코드·씬을 자동 변경하지 않는다. 이 문서에 있는 코드는 미적용 학습안이다.
## 다음 단계: 미적용 스크립트 전체 코드 — 입력과 개별 쿨타임

작성할 파일: `Assets/02.Scripts/Player/PlayerSkillController.cs`

이번 단계는 입력 승인 로그와 독립 타이머만 확인한다. 애니메이션·피해·시전 상태는 아직 연결하지 않는다. 따라서 1과 2를 연속으로 누르면 둘 다 승인되는 것이 이 단계에서는 정상이다.

아래는 직전 안내한 구현 내용이다. 작성 시작 전에 실제 파일 유무를 다시 확인하고, 사용자가 직접 작성할 수 있도록 설명한다.

```csharp
using UnityEngine;

/// <summary>
/// Skill1, Skill2 입력과 개별 쿨타임을 관리합니다.
/// 현재는 사용 조건을 검사하고 로그로 확인하는 학습 단계입니다.
/// 애니메이션, 판정, 스킬 취소는 이후 연결합니다.
/// </summary>
public class PlayerSkillController : MonoBehaviour
{
    [Header("레퍼런스")]
    // 스킬 입력 이벤트를 제공하는 컴포넌트입니다.
    [SerializeField] PlayerInputReader inputReader;
    // 사망 상태에서 사용을 차단합니다.
    [SerializeField] Health health;
    // 회피 중 사용을 차단합니다.
    [SerializeField] PlayerDodgeController dodgeCtr;

    [Header("스킬 쿨타임")]
    // 초기 테스트 설정값이며 최종 밸런스가 아닙니다.
    [SerializeField, Min(0f)] float skill1CoolTime = 5f;
    [SerializeField, Min(0f)] float skill2CoolTime = 8f;

    // 설정값과 별도로 현재 남은 시간을 저장합니다.
    float skill1CoolTimer;
    float skill2CoolTimer;

    // 이후 UI가 읽을 수 있도록 외부 변경은 허용하지 않습니다.
    public float Skill1RemainingCooldown
    {
        get { return skill1CoolTimer; }
    }
    public float Skill2RemainingCooldown
    {
        get { return skill2CoolTimer; }
    }

    // Inspector 참조가 없으면 같은 오브젝트에서 찾습니다.
    private void Awake()
    {
        if (inputReader == null)
            inputReader = GetComponent<PlayerInputReader>();
        if (health == null)
            health = GetComponent<Health>();
        if (dodgeCtr == null)
            dodgeCtr = GetComponent<PlayerDodgeController>();

        if (inputReader == null || health == null || dodgeCtr == null)
            Debug.LogError("PlayerSkillController의 레퍼런스를 확인하세요.", this);
    }

    // 활성화 시 입력 알림을 등록합니다.
    private void OnEnable()
    {
        BindInputEvents();
    }

    // 비활성화 시 등록한 입력 알림을 해제합니다.
    private void OnDisable()
    {
        UnBindInputEvents();
    }

    // 두 타이머를 프레임 소요 시간만큼 감소시킵니다.
    private void Update()
    {
        UpdateCoolTimers();
    }

    // 입력별 처리 메서드를 각각 구독합니다.
    void BindInputEvents()
    {
        if (inputReader == null)
            return;
        inputReader.Skill1Pressed += HandleSkill1Pressed;
        inputReader.Skill2Pressed += HandleSkill2Pressed;
    }

    // 동일한 이벤트와 메서드 조합으로 구독을 해제합니다.
    void UnBindInputEvents()
    {
        if (inputReader == null)
            return;
        inputReader.Skill1Pressed -= HandleSkill1Pressed;
        inputReader.Skill2Pressed -= HandleSkill2Pressed;
    }

    // 공통 사용 조건입니다. 개별 쿨타임은 각 핸들러에서 검사합니다.
    bool CanUseSkill()
    {
        if (health == null || dodgeCtr == null)
            return false;
        if (health.IsDead)
            return false;
        if (dodgeCtr.IsDodging)
            return false;
        return true;
    }

    // Skill1 조건을 통과했을 때만 사용을 승인합니다.
    void HandleSkill1Pressed()
    {
        if (!CanUseSkill())
            return;
        if (skill1CoolTimer > 0f)
        {
            Debug.Log($"Skill1 쿨타임: {skill1CoolTimer:F1}초 남음", this);
            return;
        }
        UseSkill1();
    }

    // Skill2는 Skill1과 별개의 타이머를 검사합니다.
    void HandleSkill2Pressed()
    {
        if (!CanUseSkill())
            return;
        if (skill2CoolTimer > 0f)
        {
            Debug.Log($"Skill2 쿨타임: {skill2CoolTimer:F1}초 남음", this);
            return;
        }
        UseSkill2();
    }

    // 승인 시 Skill1 타이머만 시작합니다.
    void UseSkill1()
    {
        skill1CoolTimer = skill1CoolTime;
        Debug.Log("Skill1_SlashAttack 사용 승인", this);
    }

    // 승인 시 Skill2 타이머만 시작합니다.
    void UseSkill2()
    {
        skill2CoolTimer = skill2CoolTime;
        Debug.Log("Skill2_SpinAttack 사용 승인", this);
    }

    // 음수가 되지 않도록 0으로 제한합니다.
    void UpdateCoolTimers()
    {
        skill1CoolTimer = Mathf.Max(0f, skill1CoolTimer - Time.deltaTime);
        skill2CoolTimer = Mathf.Max(0f, skill2CoolTimer - Time.deltaTime);
    }
}
```

### Inspector 연결

1. Play 종료 후 PlayerInputReader, Health, PlayerDodgeController가 붙은 Player에 PlayerSkillController 추가.
2. Input Reader → Player의 PlayerInputReader.
3. Health → Player의 Health.
4. Dodge Ctr → Player의 PlayerDodgeController.
5. Skill1 Cool Time → 5, Skill2 Cool Time → 8 (테스트 값).
6. Player Input → Events → 해당 Action Map에서 Skill1 → PlayerInputReader.OnSkill1, Skill2 → PlayerInputReader.OnSkill2 확인. 기존 항목을 중복 추가하지 않는다.

### 테스트와 완료 기준 — 모두 아직 미확인

- [ ] 컴파일 오류 없이 컴포넌트 추가 및 참조 연결.
- [ ] Game 창에서 1 입력 → Skill1_SlashAttack 사용 승인 로그.
- [ ] 바로 1 재입력 → 남은 쿨타임 출력.
- [ ] Skill1 쿨타임 중 2 입력 → Skill2_SpinAttack 승인 로그.
- [ ] 바로 2 재입력 → Skill2 남은 쿨타임 출력.
- [ ] 각각 5초/8초 경과 후 해당 스킬 재승인.
- [ ] 회피 중·사망 후 사용 승인 없음.

학습 효과: 입력 이벤트와 스킬 책임 분리, 설정값과 실행 상태 구분, 개별 타이머, 읽기 전용 프로퍼티를 통한 추후 UI 연결, 활성화/비활성화 구독 관리.

포트폴리오 설명 포인트: 구현과 테스트 후에는 “입력 장치 처리와 스킬 사용 조건을 분리하고, 두 스킬의 재사용 대기시간을 독립 관리했다. UI에는 남은 시간을 읽기 전용으로 제공했다”라고 설명할 수 있다. 현재는 미적용 설계이므로 완료 성과로 기록하지 않는다. 시전 상호 배제, 취소, 애니메이션·판정 동기화는 다음 단계의 별도 검증 대상이다.

## 입력 테스트 이후 진행할 작업

아래는 후속 계획이며 구현 완료가 아니다.

1. 현재 사용 중인 스킬 상태와 외부 읽기 구조 추가.
2. 스킬 중 다른 스킬과 기본 공격 차단, 이동 규칙 연결.
3. PlayerAnimationController와 Skill1/Skill2 Trigger 연결.
4. Skill1_SlashAttack / Skill2_SpinAttack 상태 전환 확인. Slash 상태 명칭 불일치 점검.
5. Animation Event에서 공격 범위 활성·비활성·스킬 종료 연결.
6. 회피·사망 시 취소와 판정 즉시 종료. 취소해도 시작한 쿨타임 유지하는 방향을 제안했으며 연결 단계에서 확인.
7. Enemy_Dummy 피해와 공격당 중복 피해 방지 확인. Spin은 우선 적마다 사용 1회당 피해 1회인 설계안.
8. 실제 플레이 결과에 맞춰 범위·피해량·쿨타임 조정.
9. 이펙트는 추후 추가하고 최종 시각 범위와 판정 동기화.

기본 공격의 새 모션·범위 제작은 보류한다. 다만 스킬과의 상태 충돌 차단은 필요한 통합 범위다.


---

# 문서 갱신 규칙

1. 기능을 실제로 구현하고 Unity Play Mode에서 테스트한다.
2. 정상 작동이 확인된 항목만 `[x]`로 변경한다.
3. 일부 구현 상태는 `[~]`와 함께 남은 내용을 적는다.
4. 새로운 기능을 시작하기 전에 현재 작업의 완료 기준을 먼저 확인한다.
5. 현재 주차가 끝나면 완료 내용을 `8주간의 계획서.md`에 반영한다.
6. 다음 주차 시작 시 이 문서를 해당 주차 목표로 교체한다.
7. 일정 변경 시 기능을 삭제하지 말고 이동한 주차와 이유를 기록한다.

8. 현재 중단 지점, 다음 코드 작성·연결·테스트 절차는 이 파일에 인수인계한다. 전체 범위나 일정 변경은 8주간의 계획서.md에도 반영한다.
